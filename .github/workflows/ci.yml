name: CI

on: [push]

jobs:
  build:

    runs-on: [ubuntu-latest, windows-latest, macOS-latest]
    
    steps:
    - uses: actions/checkout@v1

    - name: make
      run: make
      
#     TODO: Add these to makefile 
#    - name: make check
#      run: make check
#    - name: make distcheck
#      run: make distcheck
